#include <stdbool.h>

int smallestNumber(int n, int t) {
    while (true) {
        int temp = n;
        int digit_product = 1;
        
        while (temp > 0) {
            digit_product *= (temp % 10);
            temp /= 10;
        }
        
        if (digit_product % t == 0) {
            return n;
        }
        
        n++;
    }
}
